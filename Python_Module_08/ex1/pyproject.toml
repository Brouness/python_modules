[tool.poetry]
name = "matrix_env"
version = "0.1.0"
description = "Matrix data pipeline"
authors = ["Your Name <your@email.com>"]

[tool.poetry.dependencies]
python = ">=3.10"
pandas = ">=2.0.0"
numpy = ">=1.24.0"
requests = ">=2.34.0"
matplotlib = ">=3.7.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
